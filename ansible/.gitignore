# Secrets (main .secrets.local is at parent level)
secrets.local.yml
*.vault.yml

# Ansible
*.retry

# Logs (verbose output from make commands)
logs/

# Package cache (downloaded binaries)
.packagecache/
